# Ticketing section photo

One small overlapping photo card shown next to the QR mockup in the "At the
door" section. If the file is missing, the photo card just doesn't render —
nothing breaks.

| File | Used as |
|---|---|
| `checkin-moment.jpg` | Small 4:3-ish photo of an organizer scanning a ticket at the door |

Recommended spec: roughly 4:3 crop, at least 400×300, JPG, under ~150KB.
